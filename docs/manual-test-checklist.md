# TaskBuddy 人工测试清单

> 本文档记录由用户亲自执行的验收事项。Codex 只追加步骤与预期结果，不执行测试；用户未填写实际结果时，状态保持“待执行”。

## 长会话视口与滚动布局

- 状态：待执行
- 前置条件：本地前后端已启动，已有一个包含足够多消息、内容高度超过视口的会话。

### 桌面端 1440px

1. 将浏览器视口宽度设置为 1440px，打开长会话。
2. 在消息区域内向上、向下滚动。
3. 将消息滚动到中间位置，观察页头、输入框和侧栏。
4. 创建足够多的会话，使侧栏列表高度超过视口，再滚动侧栏列表。

预期结果：

- 浏览器 `body` 不产生用于滚动整页的纵向滚动条；
- 只有消息区域滚动，Agent 页头和 Composer 始终留在视野中；
- 左侧栏高度固定为视口高度，不随消息内容增长；
- 会话项过多时仅侧栏列表滚动，品牌区和“新建会话”按钮保持可见；
- Composer 不覆盖最后一条消息，最后一条消息可以完整滚动到可见位置。

实际结果：待填写。

## Studio 布局骨架移植验收

- 状态：待执行
- 参考实现：Spring AI Alibaba Studio `agent-chat-ui/src/components/thread/index.tsx`。

### Composer 底部定位

1. 分别打开空会话、短会话和长会话。
2. 在长会话中将消息滚动到顶部、中部和底部。
3. 调整浏览器窗口高度，并在 375px 移动视口下重复操作。

预期结果：

- Composer 始终贴住聊天主区域底部，不紧跟空状态或最后一条短消息；
- 长会话滚动时 Composer 保持可见，不随消息或 `body` 滚动；
- Agent Header 保持在主区域顶部；
- 空会话提示在 Header 与 Composer 之间的剩余空间居中；
- 消息区占据 Header 与 Composer 之间的全部剩余高度并独立滚动。

实际结果：待填写。

### 移动端 375px

1. 将浏览器视口宽度设置为 375px，打开长会话。
2. 在消息区域内滚动到历史消息和最新消息。
3. 打开侧栏并滚动较长的会话列表，然后关闭侧栏。
4. 聚焦输入框，输入多行文字并观察页面与软键盘交互。

预期结果：

- 页头和 Composer 不被长消息推离页面；
- 消息区独立滚动，输入区始终可以到达并操作；
- 移动侧栏占满当前视口高度，内部会话列表独立滚动；
- 关闭侧栏后不会改变消息区滚动边界；
- 输入多行内容时页面不会恢复为整页滚动。

实际结果：待填写。
